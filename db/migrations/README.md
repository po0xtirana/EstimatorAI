# Legacy migrations

The canonical migration source is `supabase/migrations`. Files in this directory are retained only as historical reference and must not be applied to a new environment. Add all future schema changes to `supabase/migrations` and verify them against the deployed Supabase migration history.
